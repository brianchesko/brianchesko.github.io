import React from 'react';
import Landing from '../Landing';

export default function(props) {
    return (
        <Landing>
            <h2>
                Temporary landing home.
            </h2>
        </Landing>
    );
}